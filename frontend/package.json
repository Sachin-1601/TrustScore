{
  "name": "trustscore-frontend-runner",
  "version": "1.0.0",
  "private": true,
  "description": "Pod adapter: serves the production Next.js build (located at /app) on port 3000.",
  "scripts": {
    "start": "cd /app && exec node_modules/.bin/next start -p 3000 -H 0.0.0.0"
  }
}
